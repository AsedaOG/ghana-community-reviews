# Copy to .env.local and adjust if the API runs elsewhere
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/api

# Same OAuth 2.0 Web Client ID as GOOGLE_OAUTH_CLIENT_ID on the backend.
# Leave empty to keep the "Continue with Google" button hidden.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
